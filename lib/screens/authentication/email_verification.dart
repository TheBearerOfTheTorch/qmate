import 'package:flutter/material.dart';

class EmailVerifyAuth extends StatelessWidget {
  const EmailVerifyAuth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Text("verify your email here"),
    );
  }
}
